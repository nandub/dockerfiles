name: arch-pkgbuild

on:
  workflow_dispatch:
  push:
    paths:
      - 'arch-pkgbuild/**'
      - '.github/workflows/arch-pkgbuild.yml'
  pull_request:
    paths:
      - 'arch-pkgbuild/**'
      - '.github/workflows/arch-pkgbuild.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run ShellCheck
        shell: pwsh
        working-directory: arch-pkgbuild
        run: ./scripts/Invoke-ShellCheck.ps1

      - name: Build images and run smoke package
        shell: pwsh
        working-directory: arch-pkgbuild
        run: ./scripts/Test-ArchPkgbuild.ps1

  publish:
    needs: smoke
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/master')
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Docker Hub credentials
        shell: bash
        run: |
          if [ -z "${DOCKERHUB_USERNAME}" ]; then
            echo "::error::DOCKERHUB_USERNAME is missing. Add it as a repository secret, not an environment secret or plain repository variable."
            exit 1
          fi

          if [ -z "${DOCKERHUB_TOKEN}" ]; then
            echo "::error::DOCKERHUB_TOKEN is missing. Add it as a repository secret, not an environment secret or plain repository variable."
            exit 1
          fi

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Publish full image
        uses: docker/build-push-action@v6
        with:
          context: ./arch-pkgbuild
          push: true
          tags: nandub/arch-pkgbuild:latest

      - name: Publish plain image
        uses: docker/build-push-action@v6
        with:
          context: ./arch-pkgbuild
          file: ./arch-pkgbuild/Dockerfile.plain
          push: true
          tags: nandub/arch-pkgbuild:plain
